# Claude (Claude Code / project context)

**Full agent policy:** see `AGENTS.md` in the repository root. This file exists so tools that only load `CLAUDE.md` still get the essentials.

## Planning (must follow)

- New plans → write under `plans/backlog/` as `YYYY-MM-DD-short-title.md`.
- When finished → **move** the file to `plans/complete/` (do not delete by default).
- If anything in `AGENTS.md` conflicts with a user message, follow the user’s latest explicit instruction and still keep plan files consistent with whatever you agreed to do.
