# Codex and OpenAI-style coding agents

**Canonical policy:** `AGENTS.md` in the repository root. Read that file for the full set of project agent rules.

## Planning (required)

- New plans → `plans/backlog/` as `YYYY-MM-DD-short-title.md`.
- Done → **move** to `plans/complete/`; do not delete by default.
- See `AGENTS.md` for naming details and when to create a plan.
